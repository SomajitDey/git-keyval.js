{
  "type": "module",
  "dependencies": {
    "@octokit/graphql": "^8.2.2",
    "@octokit/request": "^9.2.3",
    "js-base64": "^3.7.7",
    "mime-db-lite": "^2.1.0"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "mocha": "^11.1.0",
    "semistandard": "^17.0.0"
  },
  "scripts": {
    "test": "mocha --recursive --extension 'test.js' --timeout 40000 --parallel --check-leaks src/",
    "lint": "semistandard --fix"
  }
}
